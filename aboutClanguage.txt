c langauge : C is a most powerful programing language.
    which developed by Denis M. Ritchi in 1972 at AT and T bell lab.
    befor c program there was B programing 
    and befor  program there is BCPL(Basic Compluted Programing Language)
    ANSI standarized the c programing in 1989
    ANSI(American National Standadrd Institue)
    and rename as ANSi C and C89
-------------------------------------------------------- 
#: pre defined 
include: to add something 
<filename>:filename
<stdio.h>: Standadrd input output header file 
void main(){: opennig block 
}: closing block
printf: printf is an inbuilt function which is used to 
        display appropiate message on console screen .
-----------------------------------------
    structure of c language:
    #include<stdio.h>
    void main()
    {
    }
---------------------------------------
\n: for new line 
==================================================================================================================
DATE:24.06.2026

Datatype: "which type of value we store in a variables its called Datatype."
 
                                value
                                  |
                                  v 
------------------------------------------------------------------
|                                |                                |
v                                v                                v
numbers                         character                         true/false

numbers:
        (1)point
        (2)without point

point: book price:159.90
        height:5.2

        float        %f 
        double       %lf
        long double  %LF 

without point:  age:23
                score:80

            int (integer) %d 
-----------------------------------------------------------------------------------------------
                variable
variable : A name which can store a value. 

    e.g. bottle = water     
        here, water is my value and bottle is a variable name 

        lvalue = rvalue 
        lvalue means variablename and rvalue means value 
        use of variable in program. 

step 1 : variable declaration 
        syntax : datatypename  variablename; 
        e.g.    int  score; 

step 2 : variable defination or variable initlization 
        syntax : variablename = value;

step 3 : use of variable 
        syntax :    printf("%placeholder",variablename)
        e.g.  : printf("%d",score);
-----------------------------------------------------------
                KEYWORDS
keywords are reserved predefined words which have predefined meaning in programing

c language is a case sensetive programing langauge
e.g. 
        ASCII value
        A:65
        a:97
--------------------------------------
variable rules:
(1) we can't use predefined keywords as a variable name
        e.g.  int int x
              float float x

              int INT (correct)

(2) we can't add whitespace in variable name
        e.g. int subject score x

(3) variable namecotant only AtoZ, atoz, 0 to 9digits and _(underscore) only.
        e.g. int standard_score

(4) variable name can't start with digit
        e.g. int 01num x 
        

    